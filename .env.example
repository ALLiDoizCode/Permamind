# AO Registry Configuration
# The 43-character process ID of the deployed AO Registry Process
# Used by the 'skills publish' command to register skills in the decentralized registry
# Obtain this from the deployment output of Story 1.2 (AO Registry Process deployment)
AO_REGISTRY_PROCESS_ID=

# Network Configuration (mainnet or testnet)
AO_NETWORK=mainnet

# Arweave Gateway
ARWEAVE_GATEWAY=https://arweave.net

# AO Network API URLs (optional overrides)
# Leave empty to use default ao connect endpoints
# AO_MU_URL=https://mu.ao-testnet.xyz
# AO_CU_URL=https://cu.ao-testnet.xyz
